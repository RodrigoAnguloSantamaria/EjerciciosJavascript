const game = {title: 'The last us 2', gender: ['action', 'zombie', 'survival'], year: 2020}
 
const { title } = game;
console.log(title);

const {gender} = game;
console.log(gender[1]);

const {year} = game;
console.log(year);